#include <iostream>
#include <math.h>

double a;
double b;

int main() {
  std::cout << "";
  std::cin >> a;
  std::cout << "";
  std::cin >> b;
  double c = sqrt(pow(a, 2) + pow(b, 2));
  std::cout << c << std::endl;
}